"use strict";

// Imports
import { test1 } from './inc/_import';

const arr = [ 1, 2, 3, 4, 5 ];
const [a, b] = arr.map(i => i * 2);

console.log( test1,  a);








// doc ready
document.addEventListener("DOMContentLoaded", () => {

});